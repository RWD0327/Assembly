{
  "name": "SIGMA OS",
  "build": {
    "dockerfile": "Dockerfile"
  }, // Added a comma here
  "remoteUser": "dev",
  "postCreateCommand": "sudo pacman -S --noconfirm gcc make nano qemu nasm glfw"
}
